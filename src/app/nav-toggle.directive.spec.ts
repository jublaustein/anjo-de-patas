import { NavToggleDirective } from './nav-toggle.directive';

describe('NavToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavToggleDirective();
    expect(directive).toBeTruthy();
  });
});
